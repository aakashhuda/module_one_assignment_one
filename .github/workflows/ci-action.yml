name: CI - Assignment-2

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  checkout-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3
